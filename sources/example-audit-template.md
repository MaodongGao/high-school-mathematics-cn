# `<source-id>` 例题与内容核对

复制本文件并命名为 `sources/<source-id>-example-audit.md`。在写正文前先建立逐页条目，完成后再逐页对账。

状态只使用：

- `已收录`：题设、全部小问和主要推导已经进入正文；
- `合并整理`：与另一条合并，但本行说明合并到哪里以及保留了什么；
- `待收录`：尚未完成，来源不能标为 `complete`；
- `暂缓`：存在必须确认的歧义，来源不能标为 `complete`；
- `有意不收录`：仅用于确有充分理由的项目，必须写明具体原因。

## 逐页核对

| PDF 页 | 原稿内容 | 状态 | 当前处理或原因 |
|---|---|---|---|
| 1 | 示例：定义与第一道两问例题 | 待收录 | 尚未录入；两问必须分别核对。 |

## 覆盖结论

在这里说明：

- 所有页是否已经复核；
- 哪些重复项目被合并；
- 是否存在原稿错误、辨认困难或代理新增题；
- 当前是否满足 `complete` 的条件。
